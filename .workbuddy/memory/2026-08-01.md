# 2026-08-01

## 项目代码整理（pmproject）
用户要求整理项目代码，经 AskUserQuestion 勾选执行了三项（未选"完善.gitignore"）：

### 1. 清理垃圾/误留文件（根目录）
- `requirements.txt`：Python 依赖清单（fastapi/langchain/sqlalchemy），项目是 Node，纯误留 → 移入 ~/.Trash
- `next.config.js`：过时 CommonJS 配置（ignoreBuildErrors/ignoreDuringBuilds），与生效的 `next.config.ts` 冲突 → 移废纸篓
- `.env copy.example`：与 `.env.example` 完全重复的冗余副本 → 移废纸篓
- `src/app/api/ai/test-model.ts.bak`：备份文件（已被 *.bak gitignore）→ 移废纸篓
- 根目录 `start.sh`：MySQL 风格向导（3306/root/project_management），与达梦项目不符；`scripts/start.sh` 才是部署脚本 → 移废纸篓
- `.DS_Store`：macOS 垃圾（已 .gitignore 忽略，零价值）。沙箱禁止移废纸篓（Operation not permitted），直接 `rm -f`

### 2. 归位散落脚本
- `check_data.ts` / `check_notifications.ts` / `test-config.js` / `test-insert.js` 从根目录移到 `scripts/`
- 修正相对 import：`./src/lib/dm-helper` → `../src/lib/dm-helper`（test-insert.js 用 dmdb 包无需改）
- git 显示 `renamed` 保留历史

### 3. 删除 431MB 旧包
- `pm-project.tar`（历史项目打包，现用 docker 部署无需）→ 移入 ~/.Trash（可恢复；清空废纸篓即释放 431MB）
- 用户通过 AskUserQuestion 多选明确确认删除

### 注意
- personal_files_safety 下优先 trash（~/.Trash）；`.DS_Store` 因沙箱拦截改用 rm
- 构建产物 `next-env.d.ts` / `tsconfig.tsbuildinfo` 仍留根目录（用户未选完善 .gitignore，暂不动，不影响运行）

### 验证
- 根目录已无散落脚本/垃圾文件；git status 显示 renamed 保留脚本历史
- 之前 Docker 化改动的 Dockerfile/docker-compose.yml/.env.example/DOCKER_README.md/src/server.ts 仍在（M 状态）
